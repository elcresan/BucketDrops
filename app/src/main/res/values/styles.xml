<resources>
    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimaryDark">@color/dark_blue</item>
        <item name="colorPrimary">@color/purple</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->

    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Dialog" >
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="ToolbarStyle" parent="Theme.AppCompat.Light">
        <item name="android:colorBackground">@color/green</item>
        <item name="android:textColor">@color/dark_blue</item>
    </style>

    <style name="DatePickerItemStyle">
        <item name="android:layout_width">@dimen/bucket_picker_tv_width</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:background">@drawable/bg_picker_item_background</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/purple</item>
        <item name="android:textSize">@dimen/bucket_picker_text_size</item>
        <item name="android:drawableTop">@drawable/up_normal</item>
        <item name="android:drawableBottom">@drawable/down_normal</item>
        <item name="android:drawablePadding">@dimen/bucket_picker_drawable_padding</item>
    </style>

</resources>
